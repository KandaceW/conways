const isOutOfBounds = require("./isOutOfBounds");

function indicesAreOutOfBounds(rowIndex, columnIndex, array) {
  let lastRow = array.length;
  let lastCol = array[0].length;
  let testTop = testTop(rowIndex)
  let testBottom = 
  let testLeft = 
  let testRight = 

  if () {
    if () {
      return true;
    }
  } else {
    return false;
  }
}

function ()


module.exports = indicesAreOutOfBounds;
